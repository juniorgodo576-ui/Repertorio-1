# AGENTS.md

This repository is **empty** — a bare `git init` with no commits, files, branches, or remotes.

- No source code, manifests, configs, or tooling exist yet.
- All commands (`build`, `test`, `lint`, `typecheck`, `dev`, `deploy`) are undefined.
- No framework, language, or dependency decisions have been made.

Before working here, determine what should be scaffolded or clarify the project goals. Existing instruction files (e.g., AGENTS.md, CLAUDE.md) from sibling directories may provide context but do not apply to this repo directly.
